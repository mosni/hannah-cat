# hannah.cat — static site.
# certbot --nginx adds the 443 server blocks.
server {
    server_name hannah.cat;
    root /srv/stack/apps/hannah-cat;
    index index.html;
    location / { try_files $uri $uri/ =404; }
    listen 80;
}
